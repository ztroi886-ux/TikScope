# Telegram bot

The bot is started automatically by `python app.py` in the Render deployment.

Set `TELEGRAM_BOT_TOKEN` and `ADMIN_IDS` as environment variables.

The bot uses Telegram long polling and does not require Cloudflare or a webhook.
